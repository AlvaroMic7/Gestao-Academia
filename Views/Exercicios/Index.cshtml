@model IEnumerable<SistemaAcademia.Models.Exercicio>

@{
    ViewData["Title"] = "Exercícios";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="display-6">💪 Catálogo de Exercícios</h1>
    <a asp-action="Create" class="btn btn-dark shadow-sm">
        ➕ Novo Exercício
    </a>
</div>

<div class="card shadow-sm border-0">
    <div class="card-body p-0">
        <table class="table table-striped table-hover mb-0 align-middle">
            <thead class="table-dark">
                <tr>
                    <th class="ps-4">Exercício</th>
                    <th>Grupo Muscular</th>
                    <th>Descrição/Dica</th>
                    <th class="text-end pe-4">Opções</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model) {
                    <tr>
                        <td class="ps-4 fw-bold">
                            @Html.DisplayFor(modelItem => item.Nome)
                        </td>
                        <td>
                            <span class="badge bg-secondary">@item.GrupoMuscular</span>
                        </td>
                        <td class="text-muted small">
                            @Html.DisplayFor(modelItem => item.Descricao)
                        </td>
                        <td class="text-end pe-4">
                            <div class="btn-group" role="group">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary">✏️</a>
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary">👁️</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">🗑️</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>