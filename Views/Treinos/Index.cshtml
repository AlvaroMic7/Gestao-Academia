@model IEnumerable<SistemaAcademia.Models.Treino>

@{
    ViewData["Title"] = "Minhas Fichas";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="display-6">📋 Minhas Fichas</h1>
    <a asp-action="Create" class="btn btn-primary shadow-sm">
        ➕ Nova Ficha
    </a>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info text-center">
        Você ainda não tem fichas de treino. Clique em "Nova Ficha" para começar!
    </div>
}

<div class="row">
    @foreach (var item in Model) {
        <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm border-0">
                <div class="card-header bg-white border-bottom-0 pt-4 pb-0">
                    <h4 class="card-title text-primary fw-bold">@item.Nome</h4>
                </div>
                <div class="card-body">
                    <p class="card-text text-muted">
                        @(string.IsNullOrEmpty(item.Observacoes) ? "Sem observações" : item.Observacoes)
                    </p>
                </div>
                <div class="card-footer bg-white border-top-0 pb-4">
                    <div class="d-grid gap-2">
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-success">
                            ⚙️ Montar / Ver Exercícios
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-light text-danger">
                            🗑️ Excluir Ficha
                        </a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>